---

check: llm --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'

all:
  type: pip
  package: llm
  binary: llm
  version:
  build_dep: []
  run_dep: []
  pre:
  post: mkdir -p /home/devilbox/.config/io.datasette.llm
