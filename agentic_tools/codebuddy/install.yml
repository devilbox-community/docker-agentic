---

check: codebuddy --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'

all:
  type: npm
  package: "@tencent-ai/codebuddy-code"
  binary: codebuddy
  version: latest
  build_dep: []
  run_dep: []
  pre:
  post:
