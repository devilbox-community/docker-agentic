---

check: qwen --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' || (qwen --version; false)

all:
  type: npm
  package: "@qwen-code/qwen-code"
  binary: qwen
  version: latest
  build_dep: []
  run_dep: []
  pre:
  post:
