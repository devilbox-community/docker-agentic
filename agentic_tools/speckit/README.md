# Spec Kit

Spec Kit is GitHub's MIT-licensed spec-driven development toolkit from https://github.com/github/spec-kit (the `specify-cli` package, not older unrelated speckit packages), installing the `specify` CLI at `/opt/agentic-tools/speckit/bin/specify` for project specification workflows.
