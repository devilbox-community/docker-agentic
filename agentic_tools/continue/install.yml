---

check: continue --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' || (continue --version; false)

all:
  type: npm
  package: "@continuedev/cli"
  binary: continue
  version:
  build_dep: []
  run_dep: []
  pre:
  post:
