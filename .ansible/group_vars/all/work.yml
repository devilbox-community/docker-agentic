---

# DO NOT ALTER THIS FILE - IT IS AUTOGENERATED.

agentic_tools:
  - aider
  - claude-code
  - cline
  - codewhale
  - codex
  - continue
  - crush
  - cursor
  - gh-copilot
  - goose
  - hermes
  - llm
  - opencode
  - qwen-code
  - reasonix
agentic_tools_enabled:
  - aider
  - claude-code
  - cline
  - codewhale
  - codex
  - continue
  - crush
  - cursor
  - gh-copilot
  - goose
  - hermes
  - llm
  - opencode
  - qwen-code
  - reasonix
agentic_tools_available:
  aider:
    disabled: []
    check: aider --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: pip
      package: aider-chat
      binary: aider
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  claude-code:
    disabled: []
    check: claude --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: npm
      package: "@anthropic-ai/claude-code"
      binary: claude
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  cline:
    disabled: []
    check: cline --help 2>&1 | grep -iE 'cline' || (cline --help; false)
    all:
      type: custom
      command: curl --version >/dev/null && printf '%s\n' '#!/bin/sh' 'echo "Use Cline in VS Code; this container persists config at /home/devilbox/.config/cline"' > /usr/local/bin/cline && chmod +x /usr/local/bin/cline
      package: cline
      binary: cline
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  codewhale:
    disabled: []
    check: codewhale --version 2>&1 | grep -qiE '(codewhale|not yet)'
    all:
      type: custom
      command: curl --version >/dev/null && printf '%s\n' '#!/usr/bin/env bash' 'echo "Tool codewhale is not yet publicly available; config dir persisted at /home/devilbox/.config/codewhale"' 'exit 0' > /usr/local/bin/codewhale && chmod +x /usr/local/bin/codewhale && mkdir -p /home/devilbox/.config/codewhale
      package: codewhale
      binary: codewhale
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  codex:
    disabled: []
    check: codex --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: npm
      package: "@openai/codex"
      binary: codex
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  continue:
    disabled: []
    check: continue --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' || (continue --version; false)
    all:
      type: npm
      package: "@continuedev/cli"
      binary: continue
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  crush:
    disabled: []
    check: crush --version 2>&1 | grep -qiE '(crush|[0-9]+\.[0-9]+)'
    all:
      type: npm
      package: "@charmland/crush"
      binary: crush
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: mkdir -p /home/devilbox/.config/crush
  cursor:
    disabled: []
    check: cursor-agent --help 2>&1 | grep -iE 'cursor' || (cursor-agent --help; false)
    all:
      type: custom
      command: curl --version >/dev/null && printf '%s\n' '#!/bin/sh' 'echo "Use Cursor IDE on the host; this container persists config at /home/devilbox/.cursor"' > /usr/local/bin/cursor-agent && chmod +x /usr/local/bin/cursor-agent
      package: cursor-agent
      binary: cursor-agent
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  gh-copilot:
    disabled: []
    check: gh copilot --help 2>&1 | grep -qi copilot
    all:
      type: custom
      command: gh copilot --help >/dev/null
      package: gh-copilot
      binary: gh
      version: ''
      build_dep: []
      run_dep:
        - gh
      pre: if ! command -v gh >/dev/null 2>&1; then curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | gpg --dearmor -o /usr/share/keyrings/githubcli-archive-keyring.gpg && chmod a+r /usr/share/keyrings/githubcli-archive-keyring.gpg && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" > /etc/apt/sources.list.d/github-cli.list && apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends gh && rm -rf /var/lib/apt/lists/*; fi
      post: ''
  goose:
    disabled: []
    check: goose --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' || (goose --version; false)
    all:
      type: curl
      command: "curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | CONFIGURE=false bash && mv /root/.local/bin/goose /usr/local/bin/goose"
      package: goose
      binary: goose
      version: ''
      build_dep: []
      run_dep:
        - bzip2
        - libxcb1
        - libdbus-1-3
      pre: ''
      post: ''
  hermes:
    disabled: []
    check: hermes --version 2>&1 | grep -qiE '(hermes|not yet)'
    all:
      type: custom
      command: curl --version >/dev/null && printf '%s\n' '#!/usr/bin/env bash' 'echo "Tool hermes is not yet publicly available; config dir persisted at /home/devilbox/.config/hermes"' 'exit 0' > /usr/local/bin/hermes && chmod +x /usr/local/bin/hermes && mkdir -p /home/devilbox/.config/hermes
      package: hermes
      binary: hermes
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  llm:
    disabled: []
    check: llm --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: pip
      package: llm
      binary: llm
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: mkdir -p /home/devilbox/.config/io.datasette.llm
  opencode:
    disabled: []
    check: opencode --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: npm
      package: opencode-ai
      binary: opencode
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  qwen-code:
    disabled: []
    check: qwen --version 2>&1 | grep -iE '[0-9]+\.[0-9]+' || (qwen --version; false)
    all:
      type: npm
      package: "@qwen-code/qwen-code"
      binary: qwen
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  reasonix:
    disabled: []
    check: reasonix --version 2>&1 | grep -qiE '(reasonix|not yet)'
    all:
      type: custom
      command: curl --version >/dev/null && printf '%s\n' '#!/usr/bin/env bash' 'echo "Tool reasonix is not yet publicly available; config dir persisted at /home/devilbox/.config/reasonix"' 'exit 0' > /usr/local/bin/reasonix && chmod +x /usr/local/bin/reasonix && mkdir -p /home/devilbox/.config/reasonix
      package: reasonix
      binary: reasonix
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
