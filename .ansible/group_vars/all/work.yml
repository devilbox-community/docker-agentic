---

# GENERATED BY bin/gen-agentic-tools.py — DO NOT EDIT MANUALLY

# The following specifies the order in which extra_tools are being installed.
extra_tools:
  - openspec
  - speckit

# The following specifies which extra_tools are linked by default.
extra_tools_enabled_by_default:
  - openspec
  - speckit

extra_tools_installed_only: []

# The following specifies how extra_tools are being installed.
extra_tools_available:
  openspec:
    disabled: []
    check: openspec --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: npm
      package: "@fission-ai/openspec"
      binary: openspec
      version: latest
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
  speckit:
    disabled: []
    check: specify --version 2>&1 | grep -qE '[0-9]+\.[0-9]+'
    all:
      type: pip
      package: specify-cli
      binary: specify
      version: ''
      build_dep: []
      run_dep: []
      pre: ''
      post: ''
